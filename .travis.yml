language: node_js

sudo: true

services:
  - docker

node_js:
  - "11.12.0"

env:
  global:
    - DOCKER_COMPOSE_VERSION=1.24.0

cache:
  directories:
    - node_modules

before_install:
  - npm i -g npm@6.7.0
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
  - sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
  - sudo apt-get update
  - sudo apt-get -y -o Dpkg::Options::="--force-confnew" install docker-ce
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin

install:
  - npm install
  - export STATE=""; docker-compose --file docker-compose-tests.yml pull

script:
  - docker-mocha -f ./test/tests-structure-passing.json -t 1 -c docker-compose-tests.yml -e dendro -p 3001 --config='docker_mocha' --stats-file="output_1_thread.csv"
